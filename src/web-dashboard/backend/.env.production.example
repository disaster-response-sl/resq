# Environment Variables Template for Backend (Render)
# Copy this file and fill in the values in your Render dashboard

# ============================================
# CRITICAL: Never commit actual .env files!
# ============================================

# Server Configuration
NODE_ENV=production
PORT=5000

# Database Configuration
# IMPORTANT: Rotate your MongoDB credentials before production!
# Get new credentials from MongoDB Atlas dashboard
MONGO_URI=mongodb+srv://[NEW_USERNAME]:[NEW_PASSWORD]@cluster0.ng1rq.mongodb.net/disaster_platform?retryWrites=true&w=majority
DATABASE_NAME=disaster_platform

# JWT Configuration
# IMPORTANT: Generate a new 256-bit secret! Use: openssl rand -hex 32
JWT_SECRET=[GENERATE_NEW_SECRET_HERE]
JWT_EXPIRES_IN=24h

# Frontend URL (Your Vercel deployment URL)
FRONTEND_URL=https://[YOUR_VERCEL_SUBDOMAIN].vercel.app

# AI Integration
# Get your Gemini API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=[YOUR_GEMINI_API_KEY]

# Payment Gateway (Commercial Bank MPGS)
# Get these from Commercial Bank or use test credentials
MERCHANT_ID=TESTITCALANKALKR
API_USERNAME=merchant.TESTITCALANKALKR
API_PASSWORD=[YOUR_API_PASSWORD]
MPGS_MOCK_MODE=false

# SLUDI/eSignet Configuration
# Set to 'true' for testing, 'false' when you have real ICTA credentials
USE_MOCK_SLUDI=true
ESIGNET_SERVICE_URL=https://sludiauth.icta.gov.lk/service
ESIGNET_AUD_URL=https://sludiauth.icta.gov.lk/service/oauth/v2/token

# Get these from ICTA after registering your application
CLIENT_ID=[YOUR_CLIENT_ID_FROM_ICTA]
CLIENT_ASSERTION_TYPE=urn:ietf:params:oauth:client-assertion-type:jwt-bearer
USERINFO_RESPONSE_TYPE=jwk

# Private keys (Get from ICTA registration)
CLIENT_PRIVATE_KEY=[YOUR_CLIENT_PRIVATE_KEY_JSON]
JWE_USERINFO_PRIVATE_KEY=[YOUR_JWE_PRIVATE_KEY_JSON]

# Redirect URIs (Update with your Render backend URL)
REDIRECT_URI_USER_PROFILE=https://[YOUR_RENDER_APP].onrender.com/userprofile
REDIRECT_URI_REGISTRATION=https://[YOUR_RENDER_APP].onrender.com/registration
MOCK_RELYING_PARTY_SERVER_URL=https://[YOUR_RENDER_APP].onrender.com

# Email Configuration (Optional - for donation receipts)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=[YOUR_EMAIL@gmail.com]
EMAIL_PASS=[YOUR_APP_PASSWORD]
